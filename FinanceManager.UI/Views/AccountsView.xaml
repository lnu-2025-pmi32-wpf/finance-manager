<UserControl x:Class="FinanceManager.UI.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Storyboard x:Key="EnterAnimation">
            <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35" />
            <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.35" />
        </Storyboard>
    </UserControl.Resources>
    <UserControl.RenderTransform>
        <TranslateTransform Y="0" />
    </UserControl.RenderTransform>

    <Grid x:Name="RootGrid" Background="#071026" Margin="16" Opacity="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock Text="Accounts" FontSize="28" Foreground="White" FontWeight="Bold" />
            <TextBlock Text="Manage your financial accounts" Foreground="#94a3b8" Margin="0,4,0,12" />
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,0,16">
            <Button x:Name="BtnAdd" Content="+ Add Account" Margin="0,0,8,0" Padding="12,8" Background="#3be07a" Foreground="White" BorderBrush="Transparent" />
            <Button x:Name="BtnEdit" Content="Edit" Margin="0,0,8,0" Padding="12,8" Background="#0f4a7a" Foreground="White" BorderBrush="Transparent" />
            <Button x:Name="BtnDelete" Content="Delete" Padding="12,8" Background="#ff6b6b" Foreground="White" BorderBrush="Transparent" />
        </StackPanel>

        <!-- Accounts List -->
        <Border Grid.Row="2" Background="#0b1626" CornerRadius="6" Padding="0" Margin="0">
            <ListView x:Name="AccountsList" ItemsSource="{Binding Accounts}" SelectedItem="{Binding Selected, Mode=TwoWay}" Background="Transparent" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="#e6f0fb" Margin="8,4" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Balance" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Balance, StringFormat=C}" Foreground="#3be07a" Margin="8,4" FontWeight="SemiBold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Currency" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CurrencyCode}" Foreground="#94a3b8" Margin="8,4" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    
    </Grid>
</UserControl>

<UserControl x:Class="FinanceManager.UI.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBox x:Name="TxtSearch" Width="300" Margin="4" />
            <ComboBox x:Name="CmbType" Width="120" Margin="4">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Income" />
                <ComboBoxItem Content="Expense" />
            </ComboBox>
            <Button x:Name="BtnRefresh" Content="Refresh" Margin="4" />
            <Button x:Name="BtnExport" Content="Export CSV" Margin="4" />
            <Button x:Name="BtnAdd" Content="Add" Margin="4" />
            <Button x:Name="BtnEdit" Content="Edit" Margin="4" />
        </StackPanel>

        <ListView Grid.Row="1" x:Name="TransactionsList" Margin="0,12,0,0" ItemsSource="{Binding Transactions}" SelectedItem="{Binding Selected, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date" Width="150" DisplayMemberBinding="{Binding TransactionDateTime}" />
                    <GridViewColumn Header="Notes" Width="300" DisplayMemberBinding="{Binding Notes}" />
                    <GridViewColumn Header="Amount" Width="120" DisplayMemberBinding="{Binding Amount}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

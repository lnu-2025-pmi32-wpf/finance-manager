<UserControl x:Class="FinanceManager.UI.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:FinanceManager.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:AmountToBrushConverter x:Key="AmountToBrushConverter" />
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Filters -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" Padding="12" CornerRadius="6">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="TxtSearch" Width="360" Margin="4" />
                <ComboBox x:Name="CmbType" Width="120" Margin="4">
                    <ComboBoxItem Content="All" />
                    <ComboBoxItem Content="Income" />
                    <ComboBoxItem Content="Expense" />
                </ComboBox>
                <Button x:Name="BtnRefresh" Content="Refresh" Margin="4" />
                <Button x:Name="BtnExport" Content="Export" Margin="4" />
                <Button x:Name="BtnAdd" Content="Add transaction" Margin="4" />
            </StackPanel>
        </Border>

        <!-- Spacer -->
        <Border Grid.Row="1" Height="12" Background="Transparent" />

        <!-- Transaction cards -->
        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource PanelBrush}" CornerRadius="6" Padding="12" Margin="0,8,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="160" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding CategoryName}" FontWeight="Bold" Foreground="White" FontSize="14" />
                                        <Border Background="#FFDDE6" CornerRadius="6" Padding="4" Margin="8,0,0,0">
                                            <TextBlock Text="{Binding CategoryType}" Foreground="Black" FontSize="11" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding TransactionDateTime, StringFormat='{}{0:MMM dd, yyyy}'}" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0" />
                                    <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,2,0,0" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FormattedAmount}" FontWeight="Bold" Foreground="{Binding Amount, Converter={StaticResource AmountToBrushConverter}}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <Button Content="✏" Width="36" Height="36" Margin="4" Click="OnEditItem" />
                                    <Button Content="🗑" Width="36" Height="36" Margin="4" Click="OnDeleteItem" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
